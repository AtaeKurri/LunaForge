<?xml version="1.0" encoding="utf-8" ?>
<Node name="Folder">
	<attributes>
		<!--<leafnode />-->
		<invoke id="0" />
	</attributes>
	<args>
		<arg id="0" type="" default="">Folder Name</arg>
	</args>
	<body>
[<arg id="0" />]
	</body>
	<tail>
		
	</tail>
	<traces>
		<Trace severity="Error" type="CannotBeNull" argid="0"/> <!-- Indicated that the argument 0 cannot be null and will throw a compilation error. -->
	</traces>
	<!-- Called on the node creation. Allows for creating children to this node (example: "If" is created with an "Else" children) -->
	<!-- The root is always the created one. Each node inside these is a children. -->
	<create>
		<Node name="Else"/>
	</create>
</Node>